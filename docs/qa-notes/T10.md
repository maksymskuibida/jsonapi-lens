# QA notes — T10

**Coverage row for the wave QA report: not applicable — no observable surface.**

T10 changes `test/setup.ts`, two test files and a comment plus one `export` in
`src/i18n/index.ts`. (`vitest.config.ts` already carried `setupFiles`; it is not in the diff.) Nothing it touches is reachable from the running app: the pin writes a
`localStorage` key only inside the vitest process, and `src/i18n/index.ts`'s behaviour is unchanged
(the export is new; the negotiation order is not).

A QA agent driving the deployed site cannot distinguish this branch from `main` by any action
available to it, so an evidence file recording a browser pass would be a fabricated record rather
than a thin one — `PROCESS.md` §3 is explicit that an honest absence is better.

## What was verified instead

`docs/test-plans/T10.md` in full: the suite under four host locales, and six mutations proving each
guard actually guards. The one number stated with a caveat is case 5 — `LC_ALL=C` passed three runs
of four, the fourth failing on a pre-existing, non-locale timing flake in `test/panels.test.ts`
that is named in the spec as out of scope.
