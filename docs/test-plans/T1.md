# Test plan — T1 Plain JSON

**What this task changes.** `assertJsonApi`'s hard refusal becomes a branch: `detectShape` names what
a pasted document actually is (`jsonapi · hal · odata · jsonrpc · envelope · collection · ndjson ·
plain`) and never throws. A document that is genuinely JSON:API reads exactly as before, with no
extra click. Everything else gets a `JsonIndex` instead of a `DocumentIndex`: inferred collections
drive the same rail the JSON:API path already has, and a repeated identifier — a bare `id`-like key
defining it, a `user_id`-shaped key or a UUID/ULID/ObjectId referencing it — renders as a real link,
the way a `{type, id}` pointer already does.

## Scope

- **In scope:** `detectShape`; `buildJsonIndex` (collections, the identity graph, dangling,
  ambiguous, the node budget); the `AnchorScope` table in `ident.ts` (D1); the plain-JSON render path
  (`render-json.ts`, the `TreeAnnotations` hook in `render-value.ts`, the generalised rail); the
  paste-view shape offer and every other load path's silent fallback to `readAny`; i18n for all of
  the above.
- **Out of scope, and where it is covered instead:** request/response handling (T2); editing,
  filtering or querying JSON (not planned); cross-document identity — matching an id between two
  separately pasted documents, or between a request and its response (T2 at the earliest, and only
  as its own decision — see D2's "What this means for later tasks"); a second value-render path
  (deliberately not built — plain JSON reuses `render-value.ts`); scroll-position restoration for
  plain JSON to the same fidelity as the JSON:API `.res`-section model (see "Known limitations" in
  the QA notes — native fragment scrolling only, by design for this task).

## Cases

| # | Scenario | Expected | How verified |
|---|---|---|---|
| 1 | Valid JSON:API document | Reads exactly as before: same `DocumentIndex`, same rail, same overview, no offer shown | automated (vitest) — `test/parse.test.ts` "reads a valid JSON:API document exactly as readDocument would"; every pre-existing test in `parse.test.ts`/`render.test.ts`/`ident.test.ts`/`reverse.test.ts` passes unmodified |
| 2 | `{}` | Shape `plain`; overview says the document is empty; zero collections, zero identities | automated (vitest) — `test/shape.test.ts` "names an empty object plain"; `test/json-index.test.ts` collections/identities are empty for an empty root |
| 3 | Bare array of objects | Shape `collection`; today a hard error | automated (vitest) — `test/shape.test.ts`, `test/json-index.test.ts` "finds an array of objects…" |
| 4 | Bare array of scalars | Shape `collection`, one collection, no identities | automated (vitest) — `test/json-index.test.ts` "still counts a bare top-level array of scalars…" |
| 5 | Bare scalar or `null` | Read, rendered as one value; not an error | automated (vitest) — `test/parse.test.ts` "reads a bare scalar or null without throwing"; `test/render-json.test.ts` renders a scalar root via `renderJsonLeftover` |
| 6 | `{"data": 1}` | Shape `envelope`; today passes `assertJsonApi` and renders nothing useful | automated (vitest) — `test/shape.test.ts`, `test/parse.test.ts` "reads {\"data\": 1} as plain JSON…" |
| 7 | `data` and `errors` together | Still refused as `jsonapi`, still offered as plain JSON; `readDocument`/"Read as JSON:API anyway" still rejects it with the same message | automated (vitest) — `test/shape.test.ts` "names data and errors together an envelope"; `test/parse.test.ts` "still refuses data and errors together" |
| 8 | HAL (`_links`, `_embedded`) | Shape `hal`; `_embedded` members become collections generically; `_links.*.href` renders as a link only when `https?://`, exactly as any other value would | automated (vitest) — `test/shape.test.ts`; `test/render-json.test.ts` "still links a genuine https URL…" / "never lets a javascript: value become an href…" |
| 9 | OData (`@odata.context`, `value`) | Shape `odata`; `value` is detected as a collection generically | automated (vitest) — `test/shape.test.ts` |
| 10 | JSON-RPC (`jsonrpc`, `error`) | Shape `jsonrpc`; `error` is visible in the generic top-level-members block | automated (vitest) — `test/shape.test.ts` for detection; QA by hand for the visual placement — no dedicated errors panel was built for this shape, see "Known limitations" |
| 11 | NDJSON / JSON Lines | Shape `ndjson`, one collection of *n* records; a trailing newline and a blank line tolerated; one malformed line reports its line number and reads the rest | automated (vitest) — `test/shape.test.ts` "NDJSON / JSON Lines" describe block (4 cases) |
| 12 | Deeply nested (200+ levels) | Indexed without a stack overflow | automated (vitest) — `test/json-index.test.ts` "indexes a 200-level-deep document…" |
| 13 | 50 MB / 200k nodes | Identity inference is skipped past the budget; collections found before the cutoff are kept; overview says so | automated (vitest) — `test/json-index.test.ts` "respects a node budget…"; QA by hand for the overview note's wording and that the page does not hang on a genuinely huge paste |
| 14 | `user_id: 42` / `users` defines `id: 42` | Links to the member | automated (vitest) — `test/json-index.test.ts` "links user_id: 42 to the users member…"; `test/render-json.test.ts` "renders a resolved reference as an `<a>`…" |
| 15 | `user_id: 42` / only `orders` has `id: 42` | Dangling, not linked | automated (vitest) — `test/json-index.test.ts` "does not link user_id: 42 when the only id: 42 is in an orders collection" |
| 16 | UUID referenced from an unrelated key | Links, because UUIDs match on value alone | automated (vitest) — `test/json-index.test.ts` "links a UUID referenced from an unrelated key…"; also ULID and ObjectId |
| 17 | Two definitions of one identifier | Ambiguous entry, no `<a>` | automated (vitest) — `test/json-index.test.ts` "marks two definitions… ambiguous"; `test/render-json.test.ts` "shows an ambiguous reference as text, never as a link" |
| 18 | `id` whose value is an object or array | Not an identity candidate | automated (vitest) — `test/json-index.test.ts` "does not treat an id whose value is an object or array as a candidate" |
| 19 | Emoji, `_`, `__`, `#`, `/` in a key used for an anchor | Encoded through `ident.ts`; no duplicate DOM id | automated (vitest) — `test/render-json.test.ts` "mints a unique id for every anchor over a corpus of hostile keys"; `test/ident.test.ts` D1 corpus tests |
| 20 | Hostile value in an attribute (`<img src=x onerror=…>`, `"><script>…`) | Appears as text everywhere the task touches, including as identity-link text | automated (vitest) — `test/render-json.test.ts` "hostile values" describe block (3 cases) |
| 21 | `javascript:` value where a URL would normally link | Never becomes an `href` | automated (vitest) — `test/render-json.test.ts` "never lets a javascript: value become an href…" |
| 22 | The paste view on a non-`jsonapi` shape | Names the shape, offers "Read as plain JSON" and "Read as JSON:API anyway" | QA by hand — the offer banner's visibility, wording and both buttons' behaviour are DOM/interaction state a vitest/jsdom test cannot meaningfully exercise end to end (real click handlers wired in `main.ts`, real `history`/`location`); see `docs/qa-notes/T1.md` |
| 23 | The rail on a plain-JSON document | Lists inferred collections with counts, and the solo filter shows/hides exactly that collection's `.group` section | automated (vitest) for the data (`railEntriesForCollections`, exercised indirectly through `buildJsonIndex`'s `topLevel` collections) and for the markup (`test/render-json.test.ts` — `.group[data-type]`, `.node-row`); QA by hand for the actual click-to-filter interaction, since `applyFilter`/`setSolo` are `main.ts` DOM event wiring, not something the render-layer unit tests exercise |
| 24 | Saving a plain-JSON document and reopening it | Same document, read the same way, sensible library row summary | automated (vitest) — `test/render-json.test.ts` "librarySummary — the LibraryEntry projection, for either Lens kind" (4 cases) tests the row-summary function directly, for both `Lens` kinds; `test/parse.test.ts` "is deterministic — the same text produces an equivalent index every time" separately covers the re-indexing half of the round trip, since `store.ts` persists raw text and reindexes on load. QA by hand for the actual IndexedDB round trip through Save → Saved documents → reopen, since jsdom has no IndexedDB and this repository does not add a polyfill dependency for one test — see "Known limitations" |
| 25 | Reloading the page with a plain-JSON document open | Restored from IndexedDB via `readAny`, not `readDocument` | QA by hand — real `location`/`history`/IndexedDB on a real reload |
| 26 | Every new string | Present in `en`, `de`, `uk`; no English literal in a template | automated (vitest, indirectly) — a missing `de`/`uk` key fails `tsc`, which `npm test`'s harness would also fail to import against; `scripts/review-preflight.sh`'s hardcoded-copy check; manual read of the three catalogues during implementation |

## Edge cases enumerated

**Payload shape**

- Empty input, whitespace only, `{}`, a bare array, a bare string (doubly-encoded), a Python `dict`
  repr, a log line with JSON in the middle, a truncated document — all pre-existing `parseJson`
  behaviour, unmodified, still covered by `test/parse.test.ts`'s original suite.
- `data` absent · `data: null` · `data` as one object · `data` as an array · `data` as something not
  resource-shaped at all (`{"data": 1}`) — the last is the new case (row 6 above).
- `errors` present · `errors` and `data` together (row 7) · `meta` only — all still valid `jsonapi`.
- A duplicate `type:id` in the JSON:API path — untouched, still first-occurrence-wins (pre-existing
  test, unmodified).
- Two definitions of the *same* bare id-like key on one object (`{"id": 5, "code": 5}`) — one
  definition, not two; tested in `test/json-index.test.ts` "does not treat a duplicate id/code pair
  on the same object as two definitions".
- A numeric `42` and a string `"42"` at the same scope — kept distinct, never conflated; tested.
- **A key that merely ends in the letters `id`, with no separator or case boundary in front of it**
  (`valid`, `is_valid`, `paid`, `grid`, `void`, `android`, `hybrid`, `fluid`, `rapid`, `solid`) — must
  never be read as a `*_id`-shaped compound reference; found in round-2 review as a real wrong-link
  bug (`/valid` resolving straight to `/vals/0`) and a real dangling-panel-noise bug (`is_valid`
  appearing as a phantom `isval` chip). Tested in `test/json-index.test.ts` "a compound reference key
  needs a real boundary", alongside `user_id`/`user-id`/`userId`/`userIDs` still resolving correctly.
  Round-4 review widened the boundary in two directions and named the third deliberately unmatched
  rather than left as an unexplained gap — see the dedicated edge cases below.
- **The compound-key boundary's separator class is `_`, `-`, `.` and a literal space** (was `_`/`-`
  only) — `user.id` and `user id` now resolve exactly like `user_id`. Found in round-4 review: a dot
  or a space carries no ambiguity cost at all (`user.id` cannot be confused with `valid` the way a
  boundary-less suffix can), and it is not a theoretical case — T2's parameter decoder hands this
  module flattened and bracketed-filter keys, which use `.` as their own separator, so this shape is
  about to become ordinary in documents this tool reads. Tested in `test/json-index.test.ts`
  "recognises a dot or a space as a separator".
- **The compound-key boundary's camelCase check is Unicode-aware** (`\p{Ll}`/`\p{N}` with the `u`
  flag, was `[a-z0-9]`, ASCII only) — `користувачId` (an entirely ordinary Ukrainian camelCase key)
  and `fußId` (German, ending in `ß`) now resolve exactly like `userId`. Found in round-4 review: this
  app ships a German and a Ukrainian catalogue, and an ASCII-only boundary meant identity inference
  silently only ever worked on a key spelled in ASCII — `benutzerId` always worked, but an equally
  ordinary key written in the language the product is localised into never did, which the reviewer
  correctly called incoherent with the rest of the product. Tested in `test/json-index.test.ts`
  "recognises a non-ASCII lower-case letter as a camelCase boundary, not only a-z".
- **`userid`, `USERID` and `userids` are left deliberately unmatched — not a limitation, the correct
  trade.** No separator and no case change means nothing distinguishes these from `valid` without a
  dictionary of real container names, and "a wrong link is worse than none" is the spec's own rule.
  Recorded here so a later change does not "fix" this back into the wrong-link bug the boundary rule
  exists to prevent. Tested alongside the win below in `test/json-index.test.ts` "leaves userid,
  USERID, userids and uid deliberately unmatched".
- **`uid` now drops out of identity inference entirely, closing a round-1 loose end**, rather than
  canonicalising to the scope `u`, which nothing could ever match — a phantom-dangling identity for
  every document with a `uid` field, structurally identical to the `is_valid` bug above. This is a
  side effect of the boundary fix (no separator, no case change), not a special case written for
  `uid` specifically. `u_id`, with an explicit separator, still correctly yields scope `u` — a real
  separator is real evidence, however short the word either side of it; tested in
  `test/json-index.test.ts` "still yields scope u for the explicitly-separated u_id, unlike bare uid".
- **A repeated bare id-like value with nothing referencing it anywhere** (three `products` all coded
  `"USD"`) — not an identity at all, and specifically **not** reported as ambiguous; found in
  round-2 review (the "lone unreferenced id" exemption only fired for exactly one definition, so two
  or more fell through to the ambiguity branch). Tested in `test/json-index.test.ts` "no references
  means no identity, however many definitions" (2 cases, including that a *referenced* repeat still
  reports ambiguous correctly).
- **NDJSON is read only when its first non-blank line parses on its own; how many other lines did
  not is reported, not just where the first one was.** Found in round-2 review and given a first fix
  (a count-based gate: at least two records, at most one malformed line); round-3 review showed the
  count was the wrong axis — a broken JSON:API paste whose opener shares a line with its first
  resource, or a truncated array with no commas at all, both produced a count indistinguishable from
  a genuine short stream, so both still read as NDJSON with content silently gone (in one case, a
  whole resource). The count-based gate also *over*-corrected the other way: a real multi-thousand-
  line log with more than one truncated line was refused outright, where the original, unguarded
  code had read it as *n* records. Fixed by gating on the first non-blank line alone, with no ceiling
  on how many later lines may fail — `ShapeEvidence`'s `ndjson-lines` now carries a `skipped` count
  (not just the first bad line's number) so the message can say *how much* was dropped, however much
  that is. Tested in `test/shape.test.ts` (5 cases across both rounds): the two round-2 fixtures
  (still correctly rejected, now for the new reason), two new fixtures matching the exact round-3
  escalation (opener sharing a line with the first resource; a comma-less truncated array), and a
  many-bad-lines fixture that must now be *accepted* with the correct `skipped` count — the case the
  count-based gate got backwards.

**Identity and anchors**

- A `type`/pointer containing `_`, `__`, `#`, `/`, `.`, `:`, a space, a quote, a newline, or emoji —
  covered by the existing `ident.ts` corpus (unmodified) plus the D1-specific additions in
  `test/ident.test.ts` for the new scope table, and `test/render-json.test.ts`'s hostile-key corpus
  for the plain-JSON render path specifically.
- A `type` deliberately shaped to look like another scope's encoded body — `test/ident.test.ts`
  "cross-scope collision freedom" includes literal lookalikes (`"r_articles__1"`, `"g_articles"`,
  etc.) as segment values in every scope.
- Round-tripping: `parseAnchorId(scope, mintAnchorId(scope, segments))` returns exactly `segments`,
  in every scope, over the hostile corpus — tested.
- All seven scope prefixes are exactly two characters, an ASCII letter and `_`, with distinct first
  characters — tested (`test/ident.test.ts`).
- No static id in `index.html` begins with a scope letter and `_` — tested by parsing the shipped
  markup and checking every `[id]`.
- **A resolved reference to a definition nested two or more levels deep** (`{a:{b:{c:{id:4}}},
  c_id:4}`) — the target element exists (`test/render-json.test.ts` "anchors a definition that is
  not itself a collection member…" asserts this directly, and asserts it is a *closed* `<details>` by
  default, which is the precondition the bug lived in), but reaching it needs every ancestor
  `<details>` opened, not just the target itself — `AUTO_OPEN_DEPTH` only keeps the first level open,
  and the old `main.ts#resolveHash` only ever opened a `.res` section. Found in round-2 review; fixed
  by walking every ancestor, gated on `!shape` the same way `.res`'s own opening already was.
  **That gate was itself wrong, found in round-3 review: `rememberState` always writes
  `next.open = openRowIds()`, which is `[]` on a plain-JSON document — and `[]` is truthy, not
  absent, so `shape` read truthy on every restore and the ancestor-opening branch never ran on a
  reload.** The click path worked and hid this completely, since only a reload (or Back/Forward)
  exercises a restored `shape` at all — and this suite's own browser suite (`test/browser/run.mjs`)
  could not have caught it either: it never loaded a plain-JSON document at all before round 4 (see
  the automated-coverage note below). Fixed by moving the `!shape` guard onto the `.res` branch only
  — the ancestor branch now runs unconditionally, correctly, since a plain-JSON document never has a
  restored fold *shape* for `shape` to legitimately describe, only an artifact of the JSON:API-shaped
  state object always carrying an `open` key. `main.ts` has no vitest harness (see "Regression fixed
  in passing" below for why), so this cannot be a vitest case — but it is no longer QA-by-hand-only:
  **round-4 review escalated that gap to a blocker in its own right, and `test/browser/run.mjs` now
  has a dedicated check** ("27 plain JSON: a reference 2+ levels deep survives reload") that pastes
  this exact fixture, clicks the reference, reloads the real page, and asserts every ancestor
  `<details>` is still open afterward — proven to actually discriminate by running it against the
  round-2 commit first (fails: chain `[true,true,true]→[false,false,true]`, height `63→23`, matching
  the review's own numbers exactly) and then against the fix (passes: `[true,true,true]→[true,true,
  true]`, height unchanged). **Asserts both the state and the place, not only the state**, per a
  final round of feedback: the scenario originally gated only on the details chain, collecting and
  printing `height` without asserting it — the exact asymmetry this project's own founding defect
  was about, mirrored rather than repeated. `plainOk` now requires `clickWorked && chainSurvived &&
  heightSurvived` (height within 2px across the reload, the same tolerance `nav-scenarios.js` already
  uses for pixel drift). See `docs/evidence/T1.md`'s round-4 section for both runs.

**Injection — on every task that renders anything**

- A hostile value painted through the plain-JSON render path: `<img src=x onerror=alert(1)>`,
  `"><script>alert(1)</script>`, placed in an attribute value and in an identity-linked value —
  tested, appears as text in both places (`test/render-json.test.ts`).
- A URL whose scheme is `javascript:` where `_links.*.href` would otherwise link — tested, never
  becomes an `href`, through the same unmodified `classify()` gate the JSON:API path already relies
  on. `data:` and `vbscript:` are not separately tested here because the gate they would need to pass
  (`classify()`'s `^https?:\/\/` regex, in `format.ts`, untouched by this task) is exactly the same
  one `javascript:` already fails — this task added no new URL-from-payload code path to test
  differently.

**Scale and layout**

- A 200-level-deep document — tested, no stack overflow (the walk is iterative).
- A document past `IDENTITY_NODE_LIMIT` (200,000 nodes) — tested, identity inference is skipped and
  the overview note is shown; **not** benchmarked for actual wall-clock time on a 50 MB paste through
  the real browser — see "Known limitations".
- A collection past `EAGER_BODY_LIMIT` (2,000 members) — capped with a "N more not shown" note rather
  than building every member eagerly. Unlike the JSON:API path, there is no lazy-body toggle to defer
  full detail to on expand (no second render path was built for that); QA by hand to confirm the note
  appears and the page does not hang, not benchmarked precisely.
- **A reference that resolves *into* that truncated tail** — found in round-2 review as a real dead
  anchor: the identity graph resolves a reference to any definition regardless of member index, but a
  member past `EAGER_BODY_LIMIT` was never rendered at all, so the link's target existed nowhere in
  the DOM and clicking it did nothing, silently. Fixed by treating such a reference as `unrendered`
  text (a fourth `ScalarAnnotation` kind, alongside `resolved`/`ambiguous`/`dangling`) rather than a
  link with no target — the identity graph itself still calls it `resolved`; the render layer is what
  knows a member was cut. Tested in `test/render-json.test.ts` "a reference past EAGER_BODY_LIMIT is
  not a dead link", which also asserts the target's own anchor id genuinely has no element in the
  document, not merely that the reference isn't a link.
- Back and Forward on the **JSON:API** path: unaffected by this task, and re-verified by running the
  existing `test/browser/nav-scenarios.js` suite unmodified (see evidence). Back and Forward on the
  **plain-JSON** path: deliberately simpler for this task — native fragment scrolling only, no
  `.res`-section position-restoration model — see "Known limitations". **What is now automated is
  narrower than full parity, and is not claimed to be more:** `test/browser/run.mjs` (round 4) can
  load a plain-JSON document at all — it could not before, see "Automated browser coverage" below —
  and has one dedicated check that a reference nested past `AUTO_OPEN_DEPTH` is still visibly open
  after a real reload. It does not check scroll position, filtered-group state, or any of the other
  properties `nav-scenarios.js` checks for JSON:API; those remain genuinely unbuilt for plain JSON,
  not merely untested.

**Automated browser coverage**

- **`test/browser/run.mjs` could not load a plain-JSON document at all before round 4.** Its paste
  loop waited for `document.querySelectorAll('.res').length > 0`, which a plain-JSON document never
  has, and never clicked the shape-offer's "Read as plain JSON" button — so any plain-JSON fixture
  timed out as "the app rejected it," regardless of whether the app actually handled it correctly.
  This is not a hypothetical gap: it is the specific reason the round-3 reload regression (previous
  bullet) shipped through an entire review round with this suite reporting a clean 24/24 — the suite
  could not see the code path the regression lived in. Found and escalated to a blocker in round-4
  review.
- **Fixed by generalising the "is something rendered" check from `.res` to `#overview`** — the one
  element `render-document.ts` gives to both `renderOverview` (JSON:API) and `renderJsonOverview`
  (plain JSON), which is what makes one check work for either shape — and by clicking
  `#shape-offer-plain` when the offer banner appears, inside the same retry loop that already handled
  a slow-booting `#parse` click. Both changes live in a new shared `readFlow()` helper, used for the
  suite's original `amtrak.json` load and for the new plain-JSON check below.
- **The reload-survival scenario from the row above is now scenario 27** in `test/browser/run.mjs`'s
  own report, run last (it replaces the loaded document and reloads the page, same reason the
  existing JSON:API reload probe already ran last). Proven to actually discriminate, not merely
  assumed to: run against the round-2 commit first — **fails**, `chain [true,true,true]→[false,false,
  true]`, height `63→23`, matching the reviewer's own reported numbers exactly — then against the
  round-4 fix — **passes**, chain and height both unchanged across the reload. All 24 pre-existing
  checks passed against the round-2 commit too, confirming the new check is isolated and does not
  destabilise the rest of the suite.
- **Not done, and out of scope for this round:** `nav-harness.js`'s `NAV` vocabulary
  (`sec`/`open`/`anchorTo`/…) is built entirely around `.res`/`.res__d` and is not generalised to a
  plain-JSON `<details>` tree — scenario 27 uses raw DOM queries instead of `NAV`, deliberately, since
  forcing the two shapes through one vocabulary would either bloat `NAV` with dual-purpose logic or
  stretch its abstractions past what they mean for JSON:API. A fuller plain-JSON navigation vocabulary,
  if a future task wants one, is a new decision, not an extension of this fix.

**Localisation**

- Every new string in `en`, `de`, `uk`; no `??` fallback; no English literal in a template — checked
  by hand across all three catalogues plus `scripts/review-preflight.sh`'s mechanical check.
- German and Ukrainian plurals for every new count-bearing message use the language's own plural
  categories (`{one, other}` for German; `{one, few, many, other}` for Ukrainian), matching the
  existing catalogue's conventions.
- The two static shape-offer button labels are `static-dom.ts` bindings, so `test/i18n.test.ts`'s
  existing "matches the English catalogue exactly" assertion — unmodified — now also covers them.

**Paste and boot races**

- **A non-`jsonapi` paste lands while `boot()` is still waiting on `loadDocument()`.** `boot()`'s "a
  document was already claimed while IndexedDB was still opening" guard checked only `current`, which
  a `jsonapi` paste sets — a paste that instead shows the shape-offer banner sets `pendingOffer`
  and leaves `current` `null`, so the guard missed it and `boot()` fell through to `showView("paste")`,
  which (via the #28 fix below) unconditionally clears a pending offer. Found in round-2 review, as a
  consequence of #28's own fix; not part of T1's own feature scope in the sense of "the plain-JSON
  reading," but squarely T1's own code (`pendingOffer` did not exist before this task). Fixed by
  guarding on `current || pendingOffer`. Same "no `main.ts` harness" limitation as #27/#28 below — QA
  by hand; see `docs/qa-notes/T1.md`.

**Persistence and the privacy promise**

- Reload with a plain-JSON document open: comes back from IndexedDB, read via `readAny` — QA by
  hand (row 25).
- Save to the library and reopen: same document, sensible summary row — automated directly for the
  summary (`librarySummary`, moved to `render-document.ts` specifically so it is testable without
  `store.ts`/IndexedDB — round-2 review) and for the determinism property, QA by hand for the actual
  IndexedDB path (row 24).
- No network request carries payload content — unaffected by this task; nothing in `shape.ts`,
  `json-index.ts`, `render-json.ts` or the `main.ts` changes opens a network connection
  (`scripts/review-preflight.sh`'s network-call check confirms no `fetch`/`XMLHttpRequest`/
  `sendBeacon`/`WebSocket` was added outside `store.ts`/`share.ts`/`crypto.ts`/`worker.ts`).

## Regression fixed in passing — not part of T1's own scope

| # | Scenario | Expected | How verified |
|---|---|---|---|
| 27 | A document is stored from an earlier session; the page is loaded fresh, landing on `/` (the resume banner shows "Still open: …"); click **Back to the document** | The full document view renders — rail, overview, content — not a blank page | QA by hand, and by me directly: reproduced the blank view on the unmodified base commit (`boot()` set `current` via `readAny`/`readDocument` without ever calling `renderLoadedView`, so `showView("doc")` revealed an empty `docEl`), fixed by rendering into `docEl` and folding back to the paste view in the same branch, and confirmed fixed for a fresh load on `/`, for a reload while already on `/view`, and after a language switch (which reloads by design — see `src/i18n/index.ts`) |
| 28 | Found while verifying #27, not reported separately: type a non-`jsonapi` payload so the shape offer shows, then leave without clicking either of its buttons (a sample button, "Back to the document") to somewhere the offer no longer applies, then return to the paste view (e.g. via "New") | The stale offer must not reappear | QA by hand, and by me directly: reproduced (the offer's own visibility was never reset on any path except its own two buttons and editing the textarea), fixed by clearing it inside `showView()` itself on every call — arriving at or leaving the paste view for any reason but showing a fresh offer now always clears a stale one — and confirmed fixed by the same reproduction |

**Why neither has an automated test.** #27's buggy branch only runs when `store.ts#loadDocument()`
resolves to a real stored document, and this repository's `vitest` environment (`jsdom`) has no
`indexedDB` — `loadDocument()`'s own `catch` resolves to `null` on that, same as any other storage
failure, so the branch this bug lived in is unreachable in a unit test without either a
`fake-indexeddb` test dependency or restructuring how `main.ts` reaches storage for testability;
both are bigger than "fix in passing", so flagged to the coordinator rather than done unilaterally.
#28 does not need storage, but both share the deeper reason neither is automated: `main.ts` has no
existing `vitest` coverage of its own — every `need("...")` call at module scope requires the real
`index.html` structure to already be in the document before import, `boot()` runs immediately and
asynchronously on import, and at least one module-scope call (`window.matchMedia`) is not
implemented by jsdom at all. Building that harness from nothing is itself bigger than either fix,
so both are verified today through `test/browser/nav-scenarios.js`'s existing scope plus the manual
reproduction recorded above and in `docs/evidence/T1.md` — consistent with how every other `main.ts`
behaviour in this codebase is verified.

## What should NOT have changed

- **The JSON:API path is byte-for-byte identical, with one named, narrow exception.**
  `DocumentIndex`, `buildIndex`, `readDocument`, `assertJsonApi`, `renderDocumentView`, `groupsHtml`,
  `buildResourceBody`, and the `.res`-section scroll-restoration model itself (`openSection`,
  `applyOpenRows`, `rememberState`) are untouched. Every pre-existing test in `parse.test.ts`,
  `render.test.ts`, `ident.test.ts` and `reverse.test.ts` passes with zero edits to those tests.
  **The one exception:** `resolveHash`'s branch on a non-`.res` target (`openAncestorDetails`, added
  in round 2 and corrected in round 3) runs for *any* such target, JSON:API documents included — its
  blast radius there, swept by hand over a rendered document, is exactly `#top-level` and
  `#unresolved` where either exists (both plain `<details>`, neither inside a `.res__d` or a
  `.group`), which nothing in this app links to today. Opening either on arrival is a strict
  improvement (they were previously left closed even when landed on directly by a raw fragment), not
  a regression, but it is a real, observed behaviour change on a path this document otherwise
  describes as untouched, and `test/browser/run.mjs`'s `ID` table is entirely `[type, id]` resource
  pairs — structurally unable to exercise either id. Named here rather than left implicit in a
  blanket claim, per round-3 review.
- **`domId`/`parseDomId`/`resourceHref`/`resourceKey`/`identityFromHash`** keep their exact existing
  behaviour — the D1 scope table is additive, not a rewrite of the `r_` scope.
- **The rail's visual output for a JSON:API document is unchanged** — `renderRail` now takes
  `RailEntry[]` instead of a `DocumentIndex`, but `railEntriesForGroups` reproduces the exact same
  entries `renderRail` used to derive internally, so the rendered markup does not change.
- Back and Forward on a JSON:API document still return the content to the same place on screen — a
  regression here would be the least visible and most damaging failure mode this task could cause,
  since nothing about T1 should touch that model at all.

## Known limitations (see `docs/qa-notes/T1.md` and `docs/evidence/T1.md` for the fuller versions)

- No lazy-body deferral for plain-JSON collection members (capped at `EAGER_BODY_LIMIT`, not deferred
  to on-expand).
- No `.res`-style scroll-position restoration for the plain-JSON view — native fragment scrolling
  only.
- HAL/OData/JSON-RPC get correct shape *detection* and generically-correct rendering (collections
  found automatically, URLs safe automatically) but no bespoke chrome (no dedicated "primary
  collection" styling for OData's `value`, no dedicated errors panel for JSON-RPC's `error`).
- `canonicalScope`'s pluralisation does not know irregular plurals (`person`/`people`) or singulars
  ending in `-se` (`house`, `response`) — documented in `json-index.ts` and D2. The failure mode is
  always "renders dangling instead of linking", never a wrong link.
- The 200,000-node and 2,000-member ceilings are exercised by construction in vitest, not benchmarked
  for wall-clock time against a real multi-megabyte paste in a real browser.
